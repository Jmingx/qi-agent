# qi-agent 插件配置示例（复制为 plugins.toml 后按需修改）
#
# 规则：
# - 显式声明的插件按此配置启用/禁用
# - 未声明的插件回落各自默认开关（default_enabled）
# - 不创建本文件 = 全部用默认开关（零配置可跑）
#
# 用法示例：
# [tool_stats]
# enabled = true

[tool_stats]
enabled = false
