# 2026-08-14 阶段2.5：调试日志

## 做了什么
- 实现独立调试日志模块（debugger.py）：DebugLogger 类，格式化打印 LLM 交互全链路
- Agent 增加可选 logger 依赖注入（agent.py 仅加 4 处可选调用）
- CLI 增加 --debug 开关（argparse，默认关闭）
- 8 个新测试（含回归保护：无 logger 时行为不变）

## 怎么做的
1. TDD：写 test_debugger.py（8 用例，capsys 捕获输出）→ RED → 实现 debugger.py → GREEN
2. 修改 agent.py：__init__ 加 logger 参数 + chat() 加 4 处 `if self.logger:` 可选调用
3. 修改 cli.py：argparse 解析 --debug，注入 DebugLogger
4. 全量验证：33 passed + ruff 无错误
5. --debug 手工验收：完整链路输出（USER→REQ→RESP→TOOL→REQ→RESP→ANSWER）
6. 无 debug 回归验证：输出与之前完全一致
7. commit → tag v0.3.1

## 遇到的问题与解决
1. **ruff E501 行太长**：测试里构造 assistant_message 的字典嵌套过深超 100 字符。解决：提取为独立变量 tool_call_msg 分行构造。
2. **日志与业务分离**：用依赖注入（logger 可选参数）+ 每处 1 行 `if self.logger:`，保证核心逻辑零改动、无 logger 时行为不变。

## 验证结果
- `uv run pytest` → 33 passed ✅
- `uv run ruff check .` → All checks passed ✅
- --debug 模式：完整链路展示 ✅
- 正常模式：无日志干扰 ✅
- git tag v0.3.1 ✅

## 下一步
- 阶段 3（记忆与会话）：SQLite 持久化，重启恢复对话
- 先写技术方案 → 评审 → 开发
