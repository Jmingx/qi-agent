# 2026-08-20 安全与环境增强：路径规则 + env_info（v0.4.11）

## 做了什么
- **security_guard 扩展**：新增内置路径规则（修复真实对抗暴露的 .git 绕过漏洞）
  - `_check_sensitive_path`：shell 命令去引号 + 空格拆分 → token 逐个过 `is_sensitive_path`
  - 两层规则共存：用户黑名单（关键词）+ 内置路径规则（安全底线，始终生效）
  - `type .git\config` / `type .env` → [安全拦截]；`type README.md` → 放行
- **新增 env_info 插件**：运行时检测环境 + pre-step 幂等注入
  - 内容：OS / shell 类型 / 可用只读命令 / 路径风格
  - 幂等：`[环境信息]` marker 检查，pre-step 每 step 触发不堆积
  - clear_context 后自动重注入
- plugins.example.toml 更新

## 怎么做的（TDD）
1. 方案评审：5 个决策点全部批准（路径规则内置/去引号token化/env_info默认开/注入内容/tag v0.4.11）
2. RED：路径规则 6 用例 + env_info 4 用例 → 模块缺失/规则未实现
3. GREEN：security_guard 扩展 + env_info 实现 → 139 passed
4. 全量验证：pytest + ruff + 手工（拦截/放行/注入内容）
5. commit → tag v0.4.11

## 遇到的问题与解决
1. **test_cli_clear 断言破坏**：env_info 默认启用后，CLI 测试的 `len(agent.history) == 3` 变 4（clear 后环境消息重注入）。解决：适配断言为 4 条并断言消息顺序（环境消息最前 + 干净对话历史）——这正是"默认开插件改变全局行为"的连锁反应，测试要跟着环境走
2. **plugins.example.toml patch 失败**：记岔了文件内容（以为有 git status）——实际是 ["git push", "mkfs", "dd "]。重读文件后再 patch

## 验证结果
- `uv run pytest` → 139 passed ✅（+10：6 路径规则 + 4 env_info）
- `uv run ruff check .` → All checks passed ✅
- 手工：.git\config/.env 拦截、README.md 放行、env_info 注入内容正确 ✅
- git tag v0.4.11 ✅

## 下一步
- 真实对话复验：`type .git\config` 应拦截；模型不应再尝试 pwd/ls（env_info 生效）
- shell 白名单 Windows 适配（模型仍可能尝试未在环境提示里的命令）
- 沙箱主线：env v1.1 双重过滤（P0）→ RestrictedPython v2（P1）
- principles/08 补充"真实对抗"案例（模型绕过尝试与多层防线）
