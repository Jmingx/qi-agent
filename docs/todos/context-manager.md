# 上下文管理模块 TODO

> 阶段 C 收尾（2026-08-23）后的已知风险与后续项

## 已知风险（L3 首跑实测暴露）

- [ ] **压缩链式退化防护**（低优先级）：小窗口/长对话下压缩过频会导致
  "摘要的摘要"逐层丢细节。当前默认 128K 窗口频率极低，风险小；
  若未来支持小窗口模型（如 8K 窗口的轻量模型）需重新评估。
  候选方案：压缩后标记摘要块不可再压 / 摘要块不参与下次压缩的早期组。

## 后续项

- [ ] **sticky 写入工具**（阶段 D 上下文注入层）：目前只有 CLI /remember
      命令，agent 工具写入 sticky 留二期（context/inject.py 统一收敛）
- [ ] **评测基线**：L3 任务跑通后，把 LONG_TASKS 纳入定期回归（--all）
- [ ] **/compact 交互优化**：压缩前确认（消息较多时）、压缩后展示摘要可折叠
