# qi-agent 项目规范（AGENTS.md）

> 本文件由用户（项目所有者）定义。Hermes 在 qi-agent 目录下工作时自动加载本文件，必须无条件遵守。
> 优先级：P0 > P1。P0 不可违背；P1 除非用户当场明确豁免，否则同样必须遵守。

## P0 要求（最高优先级）

### 1. 技术原理归档（学习型项目核心资产）
- 每一个**新增功能 / 模块 / 关键机制**，在开发完成后必须产出对应的技术原理介绍文档
- 内容要求：为什么这样设计、底层工作机制、涉及的核心概念、与 Hermes / 业界主流实现的对照、踩过的坑
- 归档位置：`docs/principles/`，命名规范 `NN-<功能名>.md`（NN 为两位数序号）
- 语言：中文，面向"agent 开发小白"读者，宁可啰嗦不可跳步

### 2. 导师式设计引导
- 用户是 agent 开发的小白，本项目的学习价值与代码价值同等重要
- 每个阶段开工前：先讲解该阶段涉及的核心技术原理（用类比 + 对照 Hermes 真实源码），再引导用户参与设计决策（接口怎么定、取舍是什么、业界怎么做的）
- 禁止"直接开写"——先讲原理，让用户理解"为什么这么做"之后再进入实现

## P1 要求

### 3. 开发记录
- 每次开发会话 / 每项任务完成后，必须在 `docs/devlog/` 追加记录
- 命名：`YYYY-MM-DD-<内容摘要>.md`
- 必含内容：做了什么、怎么做的、遇到的问题与解决过程、下一步计划

### 4. 技术方案先评审，后开发
- 任何技术方案（架构设计、接口定义、新增依赖、目录结构调整、流程变更）必须先写成方案文档
- 方案交用户评审，**用户明确批准后**才能开始开发
- 禁止：未经评审直接实现、边写边改方案、评审通过后又偷偷扩大范围
- 方案文档放 `docs/plans/`，或复用 `.hermes/plans/` 下的计划文件（评审通过后归档）

### 5. 代码风格与架构约束
- **风格统一**：遵循 PEP 8；类型注解齐全（函数参数、返回值必须标注）
- **架构合理**：模块职责单一、分层清晰（CLI / agent 核心 / LLM provider / 工具 / 存储 各司其职），禁止包级循环依赖
- **拒绝超大文件**：单文件建议 ≤ 300 行，超限必须拆分（除非有充分理由并在方案中说明）
- **拒绝超大方法**：单方法建议 ≤ 50 行，超限必须拆分
- **中文注释**：关键逻辑、复杂分支、设计意图必须写中文注释；注释解释"为什么"而非复述代码

## 文档结构约定

```
qi-agent/
├── AGENTS.md              # 本规范（Hermes 自动加载）
├── qi_agent/              # 主包
├── tests/                 # 测试
├── docs/
│   ├── principles/        # 技术原理归档（P0-1）
│   ├── devlog/            # 开发记录（P1-3）
│   └── plans/             # 方案评审文档（P1-4）
└── .hermes/plans/         # Hermes 计划文件（路线图）
```

## 标准工作流程（每个阶段/功能）

1. **导师讲解**：讲清本阶段技术原理，引导设计方向
2. **用户参与设计**：讨论接口、取舍、业界做法
3. **写技术方案**：落成文档
4. **用户评审**：批准后才继续
5. **开发**：TDD（先写测试 → 实现 → 通过）
6. **归档 + 记录**：写 `docs/principles/` 原理文档 + `docs/devlog/` 开发记录
7. **提交**：git commit（conventional commits 风格，如 `feat:`, `docs:`, `refactor:`）
